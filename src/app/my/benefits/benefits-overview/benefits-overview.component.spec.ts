import {BenefitsOverviewComponent} from './benefits-overview.component';

describe('Component: BenefitsOverview', () => {
  it('should create an instance', () => {
    let component = new BenefitsOverviewComponent();
    expect(component).toBeTruthy();
  });
});
