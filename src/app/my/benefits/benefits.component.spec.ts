import {BenefitsComponent} from './benefits.component';

describe('Component: Benefits', () => {
  it('should create an instance', () => {
    let component = new BenefitsComponent();
    expect(component).toBeTruthy();
  });
});
