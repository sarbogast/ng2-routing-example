import {BenefitsNavComponent} from './benefits-nav.component';

describe('Component: BenefitsNav', () => {
  it('should create an instance', () => {
    let component = new BenefitsNavComponent();
    expect(component).toBeTruthy();
  });
});
