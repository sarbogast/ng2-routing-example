import {MarketingComponent} from './marketing.component';

describe('Component: Marketing', () => {
  it('should create an instance', () => {
    let component = new MarketingComponent();
    expect(component).toBeTruthy();
  });
});
