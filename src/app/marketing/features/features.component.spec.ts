import {FeaturesComponent} from './features.component';

describe('Component: Features', () => {
  it('should create an instance', () => {
    let component = new FeaturesComponent();
    expect(component).toBeTruthy();
  });
});
